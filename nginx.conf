server {
    listen 80;  # 80번 포트에서 요청을 듣습니다.
    server_name localhost;  # 서버 이름을 localhost로 설정

    location / {
        root /usr/share/nginx/html;  # 정적 파일의 루트 디렉토리
        try_files $uri $uri/ /index.html;  # 요청된 파일이 없으면 index.html로 리다이렉트
    }
}
